{
  "name": "saarthi",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio serverless:deploy",
    "call": "node scripts/call.js",
    "logs": "twilio serverless:logs --environment dev --tail"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.2.1",
    "dotenv": "16.4.5",
    "twilio": "5.8.0"
  },
  "devDependencies": {
    "twilio-run": "^4.2.0"
  },
  "engines": {
    "node": "22"
  }
}
